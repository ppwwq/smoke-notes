# 离线历史回放修复 0.1.4

同步前将同一实体连续、尚未尝试发送的操作合并为最新快照，保留首个基础版本，并在同一数据库事务内调整本地版本。已经尝试发送的操作不修改内容或标识，避免网络响应丢失后破坏幂等性。重试等待期间不发送该实体后续操作；每轮每实体最多发送一条。冲突时消费该实体旧队列，便签保留最新本地冲突副本。

验证：仅运行 packages/core/tests/sync.test.ts，6 项通过，覆盖 25 次积压修改只上传一次、后续版本连续、上传中继续编辑不丢失，以及重试内容不可变。没有运行电脑端测试。

交付：Cloudflare 显示新版本部署成功；Windows 0.1.4 安装退出码 0，已安装 app.asar 与构建包哈希一致，应用重新启动。手机需重新打开页面获取新版离线缓存；真实手机画面尚未观察。

边界：过去已经尝试发送、可能被服务器接收的操作仍需原样重试，不强行压缩。已同步的历史不会被回滚或删除。
