# Preview assets

These are real screenshots of `apps/web` at code commit `badefae05ea39a7d52d4f027b05e8696976e1ff5`, built with cloud configuration disabled and run in a separate local browser origin.

- `overview.png`: notebook overview with synthetic sample notes.
- `demo.gif`: an 18-second sequence of actual UI interaction states: open a note, edit it, change color, return to the notebook, and complete two to-do items.

The sequence is an animated step walkthrough, not a continuous screen recording. It does not demonstrate native Windows always-on-top, transparency, or cross-window pointer behavior. No private user notes are included.
